# Core numerical arrays and FFT-based pattern constraints.
numpy>=1.24

# YAML experiment configuration loading.
PyYAML>=6.0

# Saving pattern images, error maps, spectra, and training curves.
matplotlib>=3.7

# Differentiable renderer, autograd gradient path, and image-Jacobian loss gradients.
# NVIDIA GPU users should install the CUDA wheel first, for example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# If your CUDA/driver setup differs, use the official PyTorch selector.
torch>=2.0

# Official Mitsuba 3 physical renderer used for the black-box finite-difference path.
mitsuba>=3.6

# Unit tests.
pytest>=7.0
